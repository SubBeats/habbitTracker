# Integration test configuration with Docker Compose
spring.datasource.url=jdbc:postgresql://localhost:5432/habit_db
spring.datasource.username=habit_user
spring.datasource.password=password

# JPA configuration for integration tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Kafka configuration for tests (disabled)
spring.kafka.enabled=false
